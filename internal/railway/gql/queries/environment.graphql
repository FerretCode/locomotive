query environment($id: String!) {
	environment(id: $id) {
		projectId
		deployments {
      		edges {
        		node {
					id
          			serviceId
          			projectId
          			status
          		}
        	}
    	}
	}
}